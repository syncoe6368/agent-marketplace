# 2026-04-04 — AgentHub MVP Session

## Progress
- Checked DB: categories (6) present, agents (0), profiles (0), reviews (0)
- User hasn't run the seed SQL yet — FK error on profiles (referenced auth.users)
- Fixed seed SQL to use `auth.admin.create_user()` instead of raw profile insert
- Created `supabase/seed.sql` with proper user creation + 5 agents + reviews
- Added auth layout with noindex metadata
- Added `vercel.json` deployment config
- Updated `.env.example` with SITE_URL
- Verified production build passes (22 routes)
- Created GitHub repo: https://github.com/syncoe6368/agent-marketplace
- Pushed all code to `master` branch
- Attempted Vercel deployment via browser — not logged in, blocked
- Attempted Supabase browser access — not logged in, blocked

## Blocked
- Seed data: user needs to run seed.sql or provide service role key
- Vercel deploy: user needs to import from GitHub via vercel.com/new

## Key commits
- `aab6b0f` — SEO, legal pages, auth metadata, sitemap, vercel config
- `3686f3c` — seed.sql for sample data

## Architecture notes
- `app/` is at project root (not under src/)
- `src/` contains components, lib, types (still uses `@/` alias → `./src/*`)
- `tsconfig.json` paths: `@/*` → `./src/*`
- Next.js 16.2.1 (Turbopack), no `srcDir` support
