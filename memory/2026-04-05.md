# 2026-04-05 — Late Night Shipping Session

## Shipped: `fbce43c` — Real-time Discord Error Alerting

### What:
- `/api/errors` now sends alerts to Discord webhook on every error
- Rate-limited: 5-min cooldown per error digest+URL combo (no spam)
- Color-coded embeds: 🟡 yellow for caught errors (has digest), 🔴 red for uncaught
- Includes URL, user agent, digest, timestamp in Discord alert
- In-memory buffer retained for GET /api/errors (admin debugging)
- Updated .env.example with DISCORD_WEBHOOK_URL documentation

### Also:
- Verified domain/SSL: agenthub.syncoe.com is live with HTTPS
- Found and dropped a stash with regressions (a previous session had stripped Analytics, FeedbackWidget, revalidation, query consolidation — all reverted)
- Checklist updated: Domain ✅, SSL ✅, Error alerting ✅

### Production: https://agenthub.syncoe.com
